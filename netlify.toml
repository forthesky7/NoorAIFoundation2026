[build]
  base = "artifacts/mockup-sandbox"
  command = "export PORT=5173 && export VITE_SKIP_TYPESCRIPT_CHECK=true && export CARTOGRAPHER_DISABLE=true && export BASE_PATH=/ && pnpm install && pnpm build"
  publish = "dist"
